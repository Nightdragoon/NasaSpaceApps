<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>HeliOS — Search</title>

  <!-- Bootstrap 5 -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

  <!-- (Opcional) Bootstrap Icons para los íconos -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" rel="stylesheet">

  <style>
    :root{
      --bg: #0B1226;
      --bg-2: #0f1833;
      --panel: #121c3a;
      --panel-2:#162248;
      --text: #e6ecff;
      --muted:#a8b3d9;
      --accent:#4da3ff;
      --border: rgba(255,255,255,.08);
      --radius: 16px;
    }
    body{
      background: radial-gradient(1200px 600px at 20% -10%, #15244d 0%, transparent 60%), var(--bg);
      color: var(--text);
    }
    .search-shell{ padding-top: 32px; padding-bottom: 24px; }
    .brand-mini{ height: 28px; opacity:.9 }
    .search-bar{
      background: var(--panel);
      border: 1px solid var(--border);
      border-radius: 999px;
      padding: 10px 14px;
    }
    .search-bar .form-control{
      background: transparent; border:0; color: var(--text);
    }
    .search-bar .form-control::placeholder{ color: #93a1c7; }
    .result-card{
      background: linear-gradient(180deg, var(--panel) 0%, var(--panel-2) 100%);
      border:1px solid var(--border);
      border-radius: var(--radius);
      padding:16px;
    }
    .result-card + .result-card{ margin-top:12px; }
    .result-title{ color: var(--accent); text-decoration:none; font-weight:600; }
    .result-title:hover{ text-decoration: underline; }
    .result-snippet{ color: var(--muted); }
    .sidebar-box{
      background: linear-gradient(180deg, var(--panel) 0%, var(--panel-2) 100%);
      border:1px solid var(--border);
      border-radius: var(--radius);
      padding:16px;
    }
    .sidebar-box + .sidebar-box{ margin-top:12px; }
    .chip{
      display:inline-flex; align-items:center; gap:6px;
      background:#0f1d3c; color:#c7d4ff;
      border:1px solid var(--border);
      border-radius:999px; padding:6px 10px; font-size:.875rem; margin:4px 6px 0 0;
    }
    .related a{ color: var(--text); text-decoration:none; display:block; padding:6px 0; }
    .related a:hover{ color: var(--accent); }
    .sticky-lg{ position: static; }
     (min-width: 992px){ .sticky-lg{ position: sticky; top: 24px; } }
  </style>
</head>
<body>

<section class="search-shell">
  <div class="container-xxl">

    <!-- Top: logo + buscador -->
    <div class="row align-items-center g-3">
      <div class="col-auto">
        <img class="brand-mini" src="logo.png" alt="HeliOS">
      </div>
      <div class="col">
        <div class="search-bar d-flex align-items-center gap-2">
          <i class="bi bi-search"></i>
          <input class="form-control" placeholder="eclipse 2026 en México" />
          <button class="btn btn-sm btn-outline-light">Search</button>
        </div>
      </div>
    </div>

    <!-- Contenido -->
    <div class="row mt-3 g-3">
      <!-- Resultados -->
      <div class="col-lg-8">
        <article class="result-card">
          <div class="small text-uppercase text-secondary mb-1">helios.gov.mx</div>
          <a class="result-title h5 d-inline-block" href="#">Eclipse solar en México en 2026</a>
          <p class="result-snippet mt-2 mb-0">Resumen breve del contenido con puntos clave y una descripción corta…</p>
        </article>

        <article class="result-card">
          <div class="small text-uppercase text-secondary mb-1">cexpro.com</div>
          <a class="result-title h5 d-inline-block" href="#">Cómo observar el eclipse de 2026 en México</a>
          <p class="result-snippet mt-2 mb-0">Recomendaciones, mejores lugares y medidas de seguridad para el evento…</p>
        </article>

        <article class="result-card">
          <div class="small text-uppercase text-secondary mb-1">astro.mx</div>
          <a class="result-title h5 d-inline-block" href="#">Eventos astronómicos destacados de 2026</a>
          <p class="result-snippet mt-2 mb-0">Fechas clave, zonas de visibilidad y enlaces útiles para aficionados…</p>
        </article>
      </div>

      <!-- Sidebar -->
      <aside class="col-lg-4">
        <div class="sticky-lg">
          <div class="sidebar-box">
            <div class="d-flex justify-content-between align-items-center">
              <h6 class="m-0">Refinar búsqueda</h6>
              <i class="bi bi-sliders"></i>
            </div>
            <div class="mt-3">
              <span class="chip"><i class="bi bi-calendar-event"></i><span>Fecha</span></span>
              <span class="chip"><i class="bi bi-geo-alt"></i><span>Región</span></span>
              <span class="chip"><i class="bi bi-funnel"></i><span>Tipo</span></span>
            </div>
          </div>

          <div class="sidebar-box related">
            <h6 class="mb-2">Búsquedas relacionadas</h6>
            <a href="#"><i class="bi bi-search me-2"></i>eclipse total en México</a>
            <a href="#"><i class="bi bi-search me-2"></i>mejores lugares para eclipses</a>
            <a href="#"><i class="bi bi-search me-2"></i>calendario astronómico 2026</a>
          </div>
        </div>
      </aside>
    </div>
  </div>
</section>

</body>
</html>
